- 命名空间：`System`
- 程序集：`System.Runtime.dll`
- 继承：`Object` > `ValueType`

`public readonly struct IntPtr`

`IntPtr`，即`nint`，表示指针位宽数量的有符号整数，其范围由操作系统位宽决定。

## 字段

### Zero

- `public static readonly nint Zero = 0`

## 属性

### MinValue

- `public static nint MinValue { get; }`

### MaxValue

- `public static nint MaxValue { get; }`

### Size

进程中`IntPtr`的大小，以字节为单位。

- `public static int Size { get; } `

## 构造函数

- `public IntPtr(Int32)`
- `public IntPtr(Int64)`
- `public IntPtr(Void*)`

## 普通方法

### CompareTo

- `public int CompareTo(Object)`
- `public int CompareTo(IntPtr)`

### Equals

- `public override bool Equals(Object)`
- `public bool Equals(IntPtr)`

### GetHashCode

- `public override int GetHashCode()`

### ToPointer

将此实例的值转换为指向未指定类型的指针。

- `public void* ToPointer()`

### ToString

- `public override string ToString()`
- `public string ToString(IFormatProvider)`
- `public string ToString(String)`
- `public string ToString(String, IFormatProvider)`

### ToInt32

将此实例转换为32位有符号整数。

- `public uint ToInt32()`

### ToInt64

将此实例转换为64位有符号整数。

- `public ulong ToInt64()`

### TryFormat

- `public bool TryFormat(Span<Char>, out Int32, ReadOnlySpan<Char>, IFormatProvider)`

## 静态方法

### Abs

- `public staic nint Abs(IntPtr)`

### Add

向`IntPtr`添加偏移量，类似进入指针的迭代。

- `public static nint Add (IntPtr, Int32)`

### Subtract

向`IntPtr`减去偏移量，类似返回指针的上一个迭代。

- `public static nint Subtract (IntPtr, Int32)`

### Clamp

- `public staic nint Clamp(IntPtr, IntPtr, IntPtr)`

### CopySign

- `public static nint CopySign(IntPtr, IntPtr)`

### CreateChecked

- `public static nint CreateChecked<TOther>(TOther) where TOther : System.Numerics.INumberBase<TOther>`

### CreateSaturaing

- `public static nint CreateChecked<TOther>(TOther) where TOther : System.Numerics.INumberBase<TOther>`

### CreateTruncating

- `public static nint CreateTruncating<TOther>(TOther) where TOther : System.Numerics.INumberBase<TOther>`

### DivRem

- `public static ValueType<IntPtr, IntPtr> DivRem(IntPtr, IntPtr)`

### IsEvenInteger

- `public static bool IsEvenInteger(IntPtr)`

### IsOddInteger

- `public static bool IsOddInteger(IntPtr)`

### IsPositive

- `public static bool IsPositive(IntPtr)`

### IsNegative

- `public static bool IsNegative(IntPtr)`

### IsPow2

- `public static bool IsPow2(IntPtr)`

### Log2

- `public static IntPtr Log2(IntPtr)`

### LeadingZeroCount

- `public static IntPtr LeadingZeroCount(IntPtr)`

### TrailingZeroCount

- `public static IntPtr TrailingZeroCount(IntPtr)`

### Max

- `public static IntPtr Max(IntPtr, IntPtr)`

### MaxMagnitued

- `public static IntPtr MaxMagnitude(IntPtr, IntPtr)`

### Min

- `public static IntPtr Min(IntPtr, IntPtr)`

### MinMagnitued

- `public static IntPtr MinMagnitude(IntPtr, IntPtr)`

### Parse

- `public static IntPtr Parse(String)`
- `public static IntPtr Parse(String, NumberStyles)`
- `public static IntPtr Parse(String, IFormatProvider)`
- `public static IntPtr Parse(String, NumberStyles, IFormatProvider)`
- `public static IntPtr Parse(ReadOnlySpan<Char>, IFormatProvider)`
- `public static IntPtr Parse(ReadOnlySpan<Char>, NumberStyles, IFormatProvider)`

### TryParse

- `public static bool TryParse(String, out IntPtr)`
- `public static bool TryParse(String, IFormatProvider, out IntPtr)`
- `public static bool TryParse(String, NumberStyles, IFormatProvider, out IntPtr)`
- `public static bool TryParse(ReadOnlySpan<Char>, out IntPtr)`
- `public static bool TryParse(ReadOnlySpan<Char>, IFormatProvider, out IntPtr)` 
- `public static bool TryParse(ReadOnlySpan<Char>, NumberStyles, IFormatProvider, out IntPtr)`

### PopCount

- `public static IntPtr PopCount(IntPtr)`

### RotateLeft

- `public static IntPtr RotateLeft(IntPtr, Int32)`

### RotateRight

- `public static IntPtr RotateRight(IntPtr, Int32)`

### Sign

- `public static IntPtr Sign(IntPtr)`
