- 命名空间：`System`
- 程序集：`System.Runtime.dll`
- 继承：`Object` > `ValueType`

`public readonly struct SByte`

`SByte`表示8位有符号整数，范围为-128（-2^7^）~127（2^7^-1）。

## 字段

### MinValue

`SByte`能表示的最小值，为常量。

- `public const sbyte MinValue = -128`

### MaxValue

`SByte`能表示的最大值，为常量。

- `public const sbyte MaxValue = 127`

## 普通方法

### CompareTo

- `public int CompareTo(Object)`
- `public int CompareTo(SByte)`

任意值的`SByte`都大于`null`。

### Equals

- `public override bool Equals(Object)`
- `public bool Equals(SByte)`

### GetHashCode

- `public override int GetHashCode()`

### GetTypeCode

- `public TypeCode GetTypeCode()`

### ToString

- `public override string ToString()`
- `public string ToString(IFormatProvider)`
- `public string ToString(String)`
- `public string ToString(String, IFormatProvider)`

### TryFormat

- `public bool TryFormat(Span<Char>, out Int32, ReadOnlySpan<Char>, IFormatProvider)`

## 静态方法

### Abs

获取对象的绝对值。

- `public staic sbyte Abs(SByte)`

### Clamp

将对象的值限定在包含边界的某范围内。

- `public staic sbyte Clamp(SByte, SByte, SByte)`

### CopySign

将对象的正负符号设置为指定对象的。

- `public static sbyte CopySign(SByte, SByte)`

### CreateChecked

根据值创建`SByte`对象。

- `public static sbyte CreateChecked<TOther>(TOther) where TOther : System.Numerics.INumberBase<TOther>`

### CreateSaturaing

根据值创建`SByte`对象，如果值超出`SByte`范围则返回边界值。

- `public static sbyte CreateChecked<TOther>(TOther) where TOther : System.Numerics.INumberBase<TOther>`

### CreateTruncating

根据值创建`SByte`对象，如果值超出`SByte`范围则截断其高位字节。

- `public static sbyte CreateTruncating<TOther>(TOther) where TOther : System.Numerics.INumberBase<TOther>`

### DivRem

计算左除以右的商和余数

- `public static ValueType<SByte, SByte> DivRem(SByte, SByte)`

### IsEvenInteger

判断对象的值是否是偶数整数。

- `public static bool IsEvenInteger(SByte)`

### IsOddInteger

判断对象的值是否为奇数整数

- `public static bool IsOddInteger(SByte)`

### IsPositive

判断对象的值是否是正数。

- `public static bool IsPositive(SByte)`

### IsNegative

判断对象的值是否为负数。

- `public static bool IsNegative(SByte)`

### IsPow2

判断对象的值是否是2的幂。

- `public static bool IsPow2(SByte)`

### Log2

计算对象值的Log2

- `public static sbyte Log2(SByte)`

### LeadingZeroCount

计算对象值中前导零的数量。

- `public static sbyte LeadingZeroCount(SByte)`

### TrailingZeroCount

计算对象值中末尾零的数量。

- `public static sbyte TrailingZeroCount(SByte)`

### Max

获取两个对象中具较大值的。

- `public static sbyte Max(SByte, SByte)`

### MaxMagnitude

获取两个对象中具较大值的。

- `public static sbyte MaxMagnitude(SByte, SByte)`

### Min

获取两个对象中具较大值的。

- `public static sbyte Min(SByte, SByte)`

### MinMagnitude

获取两个对象中具较大值的。

- `public static sbyte MinMagnitude(SByte, SByte)`

### Parse

将字符串形式的数字转化为等效的`SByte`类型。

- `public static sbyte Parse(String)`
- `public static sbyte Parse(String, NumberStyles)`
- `public static sbyte Parse(String, IFormatProvider)`
- `public static sbyte Parse(String, NumberStyles, IFormatProvider)`
- `public static sbyte Parse(ReadOnlySpan<Char>, IFormatProvider)`
- `public static sbyte Parse(ReadOnlySpan<Char>, NumberStyles, IFormatProvider)`

### TryParse

将字符串转换为`SByte`类型对象，返回结果指示转换成功还是失败。

- `public static bool TryParse(String, out SByte)`
- `public static bool TryParse(ReadOnlySpan<Char>, out SByte)`
- `public static bool TryParse(String, IFormatProvider, out SByte)`
- `public static bool TryParse(ReadOnlySpan<Char>, IFormatProvider, out SByte)` 
- `public static bool TryParse(String, NumberStyles, IFormatProvider, out SByte)`
- `public static bool TryParse(ReadOnlySpan<Char>, NumberStyles, IFormatProvider, out SByte)`

### PopCount

计算对象的值中设置的位数。

- `public static sbyte PopCount(SByte)`

### RotateLeft

将对象的值循环左移一定位数。

- `public static sbyte RotateLeft(SByte, Int32)`

### RotateRight

将对象的值循环右移一定位数。

- `public static sbyte RotateRight(SByte, Int32)`

### Sign

计算对象值的符号。正时返回正数，零时返回零，负时返回负数。

- `public static int Sign(SByte)`
