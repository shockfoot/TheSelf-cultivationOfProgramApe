- 命名空间：`System`
- 程序集：`System.Runtime.dll`
- 继承：`Object` > `ValueType`

`public readonly struct Int128`

`Int128`表示128位有符号整数，范围为-2^127^~2^127^-1。

## 属性

### MinValue

- `public static Int128 MinValue { get; }`

### MaxValue

- `public static Int128 MaxValue { get; }`

### One

获取该类型的值1。

- `public static Int128 One { get; } `

### NegativeOne

获取该类型的值-1。

- `public static Int128 NegativeOne { get; } `

### Zero

获取该类型的值0。

- `public static Int128 Zero { get; } `

## 构造函数

通过指定高、低64位来初始化`Int128`类型的实例。

- `public Int128(UInt64, UInt64)`

## 普通方法

### CompareTo

- `public int CompareTo(Object)`
- `public int CompareTo(Int128)`

### Equals

- `public override bool Equals(Object)`
- `public bool Equals(Int128)`

### GetHashCode

- `public override int GetHashCode()`

### ToString

- `public override string ToString()`
- `public string ToString(IFormatProvider)`
- `public string ToString(String)`
- `public string ToString(String, IFormatProvider)`

### TryFormat

- `public bool TryFormat(Span<Char>, out Int32, ReadOnlySpan<Char>, IFormatProvider)`

## 静态方法

### Abs

- `public staic Int128 Abs(Int128)`

### Clamp

- `public staic Int128 Clamp(Int128, Int128, Int128)`

### CopySign

- `public static Int128 CopySign(Int128, Int128)`

### CreateChecked

- `public static Int128 CreateChecked<TOther>(TOther) where TOther : System.Numerics.INumberBase<TOther>`

### CreateSaturaing

- `public static Int128 CreateChecked<TOther>(TOther) where TOther : System.Numerics.INumberBase<TOther>`

### CreateTruncating

- `public static Int128 CreateTruncating<TOther>(TOther) where TOther : System.Numerics.INumberBase<TOther>`

### DivRem

- `public static ValueType<Int128, Int128> DivRem(Int128, Int128)`

### IsEvenInteger

- `public static bool IsEvenInteger(Int128)`

### IsOddInteger

- `public static bool IsOddInteger(Int128)`

### IsPositive

- `public static bool IsPositive(Int128)`

### IsNegative

- `public static bool IsNegative(Int128)`

### IsPow2

- `public static bool IsPow2(Int128)`

### Log2

- `public static Int128 Log2(Int128)`

### LeadingZeroCount

- `public static Int128 LeadingZeroCount(Int128)`

### TrailingZeroCount

- `public static Int128 TrailingZeroCount(Int128)`

### Max

- `public static Int128 Max(Int128, Int128)`

### MaxMagnitued

- `public static Int128 MaxMagnitude(Int128, Int128)`

### Min

- `public static Int128 Min(Int128, Int128)`

### MinMagnitued

- `public static Int128 MinMagnitude(Int128, Int128)`

### Parse

- `public static Int128 Parse(String)`
- `public static Int128 Parse(String, NumberStyles)`
- `public static Int128 Parse(String, IFormatProvider)`
- `public static Int128 Parse(String, NumberStyles, IFormatProvider)`
- `public static Int128 Parse(ReadOnlySpan<Char>, IFormatProvider)`
- `public static Int128 Parse(ReadOnlySpan<Char>, NumberStyles, IFormatProvider)`

### TryParse

- `public static bool TryParse(String, out Int128)`
- `public static bool TryParse(String, IFormatProvider, out Int128)`
- `public static bool TryParse(String, NumberStyles, IFormatProvider, out Int128)`
- `public static bool TryParse(ReadOnlySpan<Char>, out Int128)`
- `public static bool TryParse(ReadOnlySpan<Char>, IFormatProvider, out Int128)` 
- `public static bool TryParse(ReadOnlySpan<Char>, NumberStyles, IFormatProvider, out Int128)`

### PopCount

- `public static Int128 PopCount(Int128)`

### RotateLeft

- `public static Int128 RotateLeft(Int128, Int32)`

### RotateRight

- `public static Int128 RotateRight(Int128, Int32)`

### Sign

- `public static Int128 Sign(Int128)`
